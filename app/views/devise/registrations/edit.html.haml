%h2
  Редактирование личного профиля
= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f|
  -if !resource.errors.empty?
    .error_message
      = devise_error_messages!

  .row
    .col-md-6
      %div
        = f.label :name
        %br/
        = f.text_field :name, class:"form-control"
      %br
      %div
        -if !@user.avatar.nil?
          .row
            .col-md-3
              = f.file_field :avatar
            .col-md-3.pull-right
              Текущий аватар:
              = image_tag @user.avatar.url(:medium)
        -else
          = f.file_field :avatar
      %br
      %div
        = f.label :email
        %br/
        = f.email_field :email, class:"form-control"
      %div
        = f.label :password
        %i (Оставьте поле пустым, если не меняете пароль)
        %br/
        = f.password_field :password, class:"form-control"
      %div
        = f.label :password_confirmation
        %i (Оставьте поле пустым, если не меняете пароль)
        %br/
        = f.password_field :password_confirmation, class:"form-control"
      %div
        = f.label :current_password
        %i (Введите свой пароль для потверждения внесенных данных)
        %br/
        = f.password_field :current_password, class:"form-control"

      %br
      .pull-right
        = f.submit "Сохранить", class:"btn btn-success"
        #{link_to "Удалить аккаунт", registration_path(resource_name), class:"btn btn-danger",:data => { :confirm => "Вы уверены?" }, :method => :delete}
    .col-md-6
